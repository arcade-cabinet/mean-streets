[project]
name = "mean-streets-tooling"
version = "0.0.0"
description = "Python tooling for Mean Streets"
requires-python = ">=3.12"

[tool.uv]
package = false

[tool.uv.workspace]
members = ["workspaces/image-extraction"]
