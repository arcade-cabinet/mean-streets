<?xml version="1.0" encoding="utf-8"?>
<!--
  Explicitly opt out of Android cloud backup and device-to-device transfer
  for all app data (including the Capacitor SQLite database under /files).
  Players rebuild state on reinstall; we don't want the on-device DB to leak
  through Google-managed backups, which are not end-to-end encrypted.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
