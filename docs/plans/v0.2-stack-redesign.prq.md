---
title: v0.2 Stack Redesign — Full Rewrite
updated: 2026-04-14
status: current
priority: HIGH
timeframe: 1 week (single giant PR)
---

# Feature: v0.2 Stack Redesign

## Overview

Full game rewrite. Replaces the v0.1 quarter-card/backpack/runner model
with a stack-based turf war. All v0.1 combat, deckbuilding, and UI is
obsolete. Creative writing (names, archetypes, affiliations, taglines)
carries forward. ONE giant PR, web-only deployment.

**Authoritative specs**:
- `docs/RULES.md` (updated) — mechanics
- `docs/DESIGN.md` (updated) — vision

**Branch**: `feat/v0.2-stack-redesign`

## Constraints

- pnpm only (no npm/yarn)
- Biome lint (no ESLint)
- Max 300 LOC per file
- Capacitor 8 for Android/iOS, jeep-sqlite, Vitest, Playwright, Vite 8
- Tests: node + DOM + browser + e2e — all must pass before merge
- No debug APK firing on merge; web deploy via cd.yml on push to `main`
- Squash-merge

## Tasks

### Epic A — Tear Down (obsolete v0.1 surface)

- [ ] P1: Delete `src/sim/turf/slot-accessors.ts`
- [ ] P1: Delete `src/sim/turf/pack-resolver.ts` (different "pack"; will be replaced by new pack-economy module)
- [ ] P1: Delete `src/ui/cards/ModifierSlot.tsx` + tests
- [ ] P1: Delete `src/ui/cards/ModifierBadge.tsx` + tests
- [ ] P1: Delete `src/ui/deckbuilder/BackpackRail.tsx` + tests
- [ ] P1: Delete `src/ui/screens/BuildupScreen.tsx` + tests (no buildup phase in v0.2)
- [ ] P1: Delete `config/raw/cards/special.json` (backpack special type is gone)
- [ ] P1: Strip runner/backpack/quarter-card code from `src/sim/turf/board.ts`, `attacks.ts`, `environment.ts`, `ai/*`
- [ ] P1: Strip `PackedBackpackDraft`, `PackedBackpackSlot` from `src/ui/deckbuilder/index.ts`

### Epic B — Types Overhaul

- [ ] P1: Rewrite `src/sim/turf/types.ts`:
  - `type Rarity = 'common' | 'rare' | 'legendary'`
  - `type CardCategory = 'tough' | 'weapon' | 'drug' | 'currency'`
  - `interface ToughCard { kind: 'tough', id, name, tagline, archetype, affiliation, power, resistance, rarity, abilities[] }`
  - `interface WeaponCard { kind: 'weapon', id, name, tagline, category, power, resistance, rarity, abilities[] }` (same shape for drug/currency)
  - `type Card = ToughCard | WeaponCard | DrugCard | CurrencyCard`
  - `interface Turf { id, stack: Card[], sickTopIdx?: number | null }` (track sick per stack position)
  - `interface PlayerState { turfs: Turf[], hand: Card[], deck: Card[], discard: Card[], toughsInPlay: number, actionsRemaining: number }`
  - `type DifficultyTier = 'easy' | 'medium' | 'hard' | 'nightmare' | 'ultra-nightmare'`
  - `interface GameConfig { difficulty: DifficultyTier, suddenDeath: boolean, turfCount: number, actionsPerTurn: number, firstTurnActions: number }`

- [ ] P1: Rewrite `src/sim/cards/schemas.ts`:
  - `RaritySchema`, `AuthoredToughSchema` (power+resist history, rarity, abilities), `AuthoredWeaponSchema`, `AuthoredDrugSchema`, `AuthoredCurrencySchema`
  - `CompiledCardSchema` unified across types
  - Remove `AuthoredBackpackSchema`, `CardSpecialSchema`

- [ ] P2: Update `src/sim/cards/compile.ts` (or equivalent) to compile the new authored formats

### Epic C — Sim Engine Rewrite

- [ ] P1: Rewrite `src/sim/turf/board.ts`:
  - `createTurf()`, `addToStack(turf, card)`, `removeFromStack(turf, idx)`
  - `positionPower(turf)`, `positionResistance(turf)` — aggregate over stack, respect `sickTopIdx`
  - `turfAffiliationConflict(turf, incoming)` — check rival graph with buffer rule
  - `seizeTurf(defender, defenderTurfIdx, attacker)` — remove turf, transfer modifiers to attacker's chosen turf
- [ ] P1: Rewrite `src/sim/turf/attacks.ts`:
  - `resolveDirectStrike(attackerTurf, defenderTurf) → StrikeResult` (kill | sick | busted)
  - `resolvePushedStrike` — spend 1 currency from attacker turf, +denomination/100 power, splash sicks one beneath
  - `resolveFundedRecruit` — sum currency on attacker turf vs target resistance × affiliation multiplier
  - Apply `strike-bottom` / `strike-anywhere` abilities (Shark, Ghost, legendaries)
  - Transfer modifiers on kill with affiliation re-eval on transfer
- [ ] P1: Rewrite `src/sim/turf/environment.ts`:
  - `stepAction(state, action)` with action types: `play_card`, `direct_strike`, `pushed_strike`, `funded_recruit`, `discard`, `end_turn`
  - Draw-gate enforcement: reject `play_card(modifier)` when `toughsInPlay === 0`
  - First-turn action budget (5) vs normal budget (3/4/3 by difficulty)
- [ ] P1: Rewrite game loop in `src/sim/turf/game.ts` (or equivalent):
  - `createMatch(config: GameConfig) → MatchState`
  - `runTurn(state, actions) → MatchState`
  - `isGameOver(state) → winner | null` — winner = opponent with 1+ turfs when other has 0

### Epic D — AI Rewrite

- [ ] P1: Rewrite `src/sim/turf/ai/planner.ts`:
  - Action enumeration for new action set
  - Score function accounts for stack value (power + resistance + tough count) and opponent stack threat
  - Affiliation compatibility check when selecting play_card targets
- [ ] P1: Rewrite `src/sim/turf/ai/policy.ts`:
  - Per-difficulty `selectAction(scored, difficulty, rng)`:
    - easy: top-5 sample, 30% noise
    - medium: top-3, 15% noise
    - hard: top-2, 5% noise, +1 action
    - nightmare: best, 0% noise, −1 player action
    - ultra-nightmare: 2-ply lookahead + best
- [ ] P2: Rewrite `src/sim/turf/ai/scoring.ts` weights in JSON config (see Epic G)

### Epic E — Data Rewrite

- [ ] P1: Update `config/raw/cards/toughs/*.json` to new schema (add rarity, drop runner-related fields)
- [ ] P1: Update `config/raw/cards/weapons/*.json` — add rarity, unified offense/defense
- [ ] P1: Update `config/raw/cards/drugs/*.json` — add rarity
- [ ] P1: Create `config/raw/cards/currency/*.json` — $100 common, $1000 rare
- [ ] P1: Delete `config/raw/cards/special.json`
- [ ] P1: Update `src/data/pools/affiliations.json` with rival/loyal/neutral/mediator directed graph
- [ ] P2: Recompile via `pnpm run build:cards` (or equivalent); verify `config/compiled/*.json` outputs

### Epic F — Koota ECS + Config

- [ ] P1: Rewrite `src/ecs/traits.ts`:
  - Replace quarter-card traits with `CardInStack`, `TurfOwner`, `SickFlag`, `AffiliationSymbol` traits
  - Keep `GameState`, `ActionBudget` traits (reshape as needed)
- [ ] P1: Rewrite `src/ecs/actions.ts` to call new sim functions
- [ ] P1: Update `src/ecs/hooks.ts` — `usePlayerTurfs`, `useActionBudget`, `useTurfStackComposite`
- [ ] P1: Extract all hardcoded tunables to `src/data/ai/turf-sim.json`:
  - `difficulty.{tier}.turfCount`, `.actionsPerTurn`, `.firstTurnActions`, `.aiNoise`, `.aiTopK`
  - `packs.{size}.rarityWeights`
  - `packs.suddenDeathBonus`
  - `combat.pushedDenominationScale`
  - `combat.sickHalfThreshold`
  - `combat.affiliationMult.{freelance|same|rival|other}`
  - `starterCollection.{toughPacks, weaponPacks, drugPacks, currencyPacks}`

### Epic G — UI Rewrite

- [ ] P1: Rewrite `src/ui/cards/CrewCard.tsx` → `src/ui/cards/Card.tsx`:
  - MTG-style frame (all card types)
  - Affiliation hero image top-left (mobile) / centered portrait (desktop)
  - Rarity badge (common=grey, rare=blue, legendary=gold)
  - Power top-right (tough/weapon/drug), Resistance bottom-right (tough/weapon/drug)
  - Currency denomination ($100 / $1000) replaces power/resist for currency cards
- [ ] P1: Rewrite `src/ui/cards/CardFrame.tsx` — MTG-style SVG frame with rarity tinting
- [ ] P1: Create `src/ui/affiliations/AffiliationSymbol.tsx`:
  - Hand-drawn SVG symbols per affiliation (one per affiliation in pool)
  - Bound to Koota trait — glow red on rival-pair, gold on loyal-pair
- [ ] P1: Rewrite `src/ui/board/` — replace `BoardLayout.tsx`, `PositionSlot.tsx` with:
  - `TurfView.tsx` — shows dynamic composite "top card" per turf
  - `TurfCompositeCard.tsx` — aggregated Power/Resistance, roster list, affiliation icon row
  - `StackFanModal.tsx` — tap/swipe to fan through stack members
- [ ] P1: Rewrite `src/ui/screens/CombatScreen.tsx` → `GameScreen.tsx`:
  - 3-turf (or N-turf) horizontal row per player
  - Action menu: Play / Direct / Pushed / Recruit / End Turn
  - Action budget counter (3/4/5 depending on turn+difficulty)
- [ ] P1: Delete `src/ui/screens/DeckBuilderScreen.tsx` (no deckbuilding)
- [ ] P1: Create `src/ui/screens/CollectionScreen.tsx` — view unlocked cards, browse by rarity/category
- [ ] P1: Create `src/ui/screens/PackOpeningScreen.tsx` — reveal flow for earned packs
- [ ] P1: Create `src/ui/screens/DifficultyScreen.tsx` — 2×3 icon grid with Sudden Death toggle
- [ ] P1: Update `src/ui/screens/MainMenuScreen.tsx` — wire New Game → DifficultyScreen, Collection button, Packs button
- [ ] P2: Delete `src/ui/hand/` quarter-card rendering; replace with simple full-card hand row

### Epic H — Pack Economy

- [ ] P1: Create `src/sim/packs/types.ts`:
  - `PackKind = 'tough-5' | 'weapon-5' | 'drug-5' | 'currency-5' | 'single' | 'triple'`
  - `PackInstance { kind, contents: Card[], earnedFor: string }`
- [ ] P1: Create `src/sim/packs/generator.ts`:
  - `generatePack(kind, collection, rng, options)` — roll rarity per card (70/25/5), pick from catalog
  - Sudden Death bonus: 30% per-card rarity bump on win
- [ ] P1: Create `src/platform/persistence/collection.ts`:
  - `loadCollection()` / `saveCollection(cards)` — SQLite via existing persistence layer
  - Starter grant on first run: 4 tough-5 + 1 each weapon-5/drug-5/currency-5
- [ ] P1: Wire pack awards into match-end flow: on win, award pack(s) based on difficulty + sudden death flag
- [ ] P2: Add `useCollection()` hook for UI

### Epic I — Autobalance Rewrite

- [ ] P1: Rewrite `src/sim/analysis/benchmark.ts` to run matches with the new sim engine
- [ ] P1: Rewrite `src/sim/analysis/sweep.ts` — per-card sweep now weighted by rarity drop rate
- [ ] P1: Rewrite `src/sim/analysis/lock.ts`:
  - Saturate at `maxHistoryLength` (keep, but raise default to 8 now that rarity bands widen the pass band)
  - Reject stat promotions that would cross a rarity band without promoting rarity grade
  - Output new `sim/reports/turf/balance-history.json` format
- [ ] P1: Update `src/sim/turf/__tests__/release-gate.test.ts` to the new balance model
- [ ] P2: Run `pnpm run analysis:benchmark` — verify winrate 48–52% for Medium baseline
- [ ] P2: Run `pnpm run analysis:lock:persist` — produce a fresh baseline

### Epic J — Tests

- [ ] P1: Rewrite node-level tests under `src/sim/turf/__tests__/`:
  - `types.test.ts`, `board.test.ts`, `attacks.test.ts`, `environment.test.ts`, `game.test.ts`
  - `ai/planner.test.ts`, `ai/policy.test.ts`
  - `affiliation-graph.test.ts` (buffer rule)
  - `draw-gate.test.ts`
  - `strike-position.test.ts` (top/bottom/anywhere)
- [ ] P1: Rewrite DOM tests for new UI components
- [ ] P1: Rewrite browser tests via `renderInBrowser` for Card / TurfCompositeCard / StackFanModal / DifficultyScreen
- [ ] P1: Rewrite e2e `app-flow.spec.ts` for new flow: menu → difficulty → game → pack reward
- [ ] P1: Keep `e2e/layout-classification.spec.ts`, `e2e/responsive-alignment.spec.ts`, `e2e/accessibility.spec.ts` — update selectors
- [ ] P1: Create `e2e/pack-opening.spec.ts`
- [ ] P1: Create `e2e/difficulty-grid.spec.ts`
- [ ] P2: Update `e2e/visual-fixtures.spec.ts` — new fixture set

### Epic K — Docs & Release

- [ ] P1: Rewrite `docs/ARCHITECTURE.md` — new module map
- [ ] P1: Rewrite `docs/PRODUCTION.md` — v0.2 release checklist
- [ ] P1: Update `docs/VISUAL_REVIEW.md` — new screens
- [ ] P1: Update `CLAUDE.md` — v0.2 surface area
- [ ] P1: Update `README.md` with new gameplay summary
- [ ] P2: Update `CHANGELOG.md` — v0.2.0 entry
- [ ] P1: Keep v0.1 assets (`public/assets/logo.png`, icons) — reuse as-is

## Dependencies

- Epic A (tear down) must complete before Epic B (types) to avoid phantom references
- Epic B before Epic C (sim depends on types)
- Epic C before Epic D (AI calls sim)
- Epic E (data) can run parallel to Epic C after Epic B locks schemas
- Epic F (ECS) depends on Epic C
- Epic G (UI) depends on Epic F
- Epic H (packs) can run parallel to Epic G after Epic E
- Epic I (autobalance) depends on Epic C
- Epic J (tests) final; depends on all above
- Epic K (docs) last

## Acceptance Criteria

- `pnpm run lint` clean
- `pnpm run test` green (all three suites)
- `pnpm run test:e2e` green on iphone-14, pixel-7, ipad-pro-landscape, desktop-chromium
- `pnpm run build` produces `dist/` with no errors
- `pnpm run analysis:benchmark` shows 48–52% AI-vs-AI winrate on Medium difficulty
- No reference to backpack / runner / quarter-card / pocket / reserve / buildup in any `.ts`/`.tsx` (grep-clean)
- One squash commit on merge
- `cd.yml` auto-fires Pages deploy on merge

## Technical Notes

- Hero images: `public/assets/affiliations/{id}.svg` — hand-drawn per affiliation. Start with geometric placeholders; editorial pass can replace.
- Rarity colors: tailwind `slate-400` / `sky-500` / `amber-500` (common / rare / legendary). Defined as CSS vars.
- SQLite schema migration: wipe v0.1 `cards`/`decks` tables, add `collection`/`packs_pending` tables. Users with v0.1 saves get a fresh start.
- Keep `scripts/generate-mobile-icons.sh` — icon set unchanged.

## Risks

- **Scope**: 1-week estimate assumes autonomous execution via `/task-batch`.
  Manual scope is 3–4 weeks.
- **Card data migration**: 100 toughs + 50 weapons + 50 drugs need schema transform. Write a one-shot migration script under `scripts/migrate-v01-to-v02.mjs` that reads v0.1 authored JSON and emits v0.2 JSON.
- **AI re-tuning**: per-difficulty AI noise values are initial guesses. Plan on a tuning pass after first clean benchmark.
- **Visual polish**: MTG-style card frame is SVG-heavy. Budget time to tune GrittyFilters for the new frame.
