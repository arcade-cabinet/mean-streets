---
title: v0.3 Task Batch — Executable Epic List
created: 2026-04-17
batch_name: v0.3-full-rewrite
base_branch: main (after PR #3 merges)
feature_branch: feat/v0.3-full-rewrite
config:
  execution_mode: parallel_specialists
  priority_strategy: auto_by_dependencies
  stop_on_failure: non_blocking_only
  completion: open_pr_to_main
---

# v0.3 Full Rewrite — Task Batch

Companion to `docs/plans/v0.3-impl.prq.md`. This file is the
batch-runnable form: explicit acceptance criteria per task, owners
preassigned, dependency DAG resolved, and precondition gates
defined.

**Do NOT start this batch until PR #3 is squash-merged to main.**
Branch `feat/v0.3-full-rewrite` from post-merge main.

## Precondition Gate

- [ ] **P0.1** — PR #3 merged to main (handled by `a7691448454e2f707`
  agent).
- [ ] **P0.2** — New branch `feat/v0.3-full-rewrite` created off
  main.
- [ ] **P0.3** — `pnpm run build` clean on base branch.
- [ ] **P0.4** — `pnpm run test` green on base branch.

Verification:
```bash
git checkout main && git pull
git checkout -b feat/v0.3-full-rewrite
pnpm install && pnpm run build && pnpm run test
```

Only once P0.1 through P0.4 pass does the batch begin.

---

## Tasks — Dependency-Ordered

### Task 1 — Epic A: Types & Shape

- **id**: v3-A-types
- **owner**: Rex
- **priority**: P1 (blocking)
- **dependencies**: [P0]
- **files**:
  - `src/sim/turf/types.ts` (rewrite)
- **description**: Add `Rarity = 'common' | 'uncommon' | 'rare' | 'legendary' | 'mythic'`; `CardInstance { cardId, rolledRarity, unlockDifficulty }`; HP + maxHP on ToughCard; StackedCard ownership; Turf.isActive + Turf.reserveIndex; new action kinds; TurfGameState.heat + blackMarket + holding + lockup + mythicPool + mythicAssignments; WarStats.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] File < 300 LOC
  - [ ] `Rarity` union includes 'mythic'
  - [ ] `PlayerState` has no `hand` field
  - [ ] `TurfGameState` has `heat`, `blackMarket`, `holding`, `lockup`, `mythicPool`, `mythicAssignments`
  - [ ] Break notes delivered to Maya/Kira/Iris/Ollie listing all files that reference removed/reshaped types

---

### Task 2 — Epic B: Sim Core (single-lane + heat + market + holding)

- **id**: v3-B-sim-core
- **owner**: Maya
- **priority**: P1
- **dependencies**: [v3-A-types]
- **files**:
  - `src/sim/turf/board.ts` (rewrite)
  - `src/sim/turf/attacks.ts` (rewrite)
  - `src/sim/turf/resolve.ts` (rewrite)
  - `src/sim/turf/environment.ts` (rewrite)
  - `src/sim/turf/game.ts` (rewrite)
  - `src/sim/turf/env-query.ts` (rewrite)
  - `src/sim/turf/heat.ts` (new)
  - `src/sim/turf/market.ts` (new)
  - `src/sim/turf/holding.ts` (new)
- **description**: Single-lane progression (active + reserves). HP + tiered damage. Modifier swap action. Black Market module (send/trade/heal/return/wipe). Holding module (send/check/bribe/lockup/return). Heat module (rarity + concentration contributions; raid probability). Resolution: raid-first then combat. Modifier ownership, no orphans, seize reconciliation.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] Each file < 300 LOC
  - [ ] `pnpm run lint src/sim/turf/` clean
  - [ ] `promoteReserveTurf(player)` exported and invoked on seize
  - [ ] `heat.ts::computeHeat` returns HeatBreakdown per RULES §10.1
  - [ ] `market.ts::sendToMarket`, `tradeAtMarket`, `healAtMarket`, `returnFromMarket`, `wipeMarket` implemented
  - [ ] `holding.ts::sendToHolding`, `holdingCheck`, `bribeSuccess`, `returnFromHolding`, `lockupProcess` implemented
  - [ ] `resolve.ts::resolvePhase` runs raid before combat (strict order)
  - [ ] Bribe probabilistic per RULES §10.3 ($500/$1000/$2000/$5000)
  - [ ] Damage tiers per RULES §7 (glance/wound/serious/crushing)
  - [ ] HP clamping applied in positionPower/positionResistance

---

### Task 3 — Epic C: Abilities

- **id**: v3-C-abilities
- **owner**: Kira
- **priority**: P1
- **dependencies**: [v3-A-types]
- **files**:
  - `src/sim/turf/abilities.ts` (extend)
  - `src/sim/turf/abilities-effects.ts` (extend)
- **description**: Rarity-scaled tangible/intangible effects. New abilities: PATCHUP, FIELD_MEDIC, RESUSCITATE, LAUNDER, LOW_PROFILE, CLEAN_SLATE, STRIKE_TWO, CHAIN_THREE, STRIKE_RETREATED, IMMUNITY, NO_REVEAL, ABSOLUTE, INSIGHT, BUILD_TURF, TRANSCEND. Probabilistic bribes.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] Each file < 300 LOC
  - [ ] Bribe returns IntangibleOutcome based on probability roll tied to seeded rng
  - [ ] ABILITY_INDEX contains all new ability keys
  - [ ] Unit test `abilities-intangibles.test.ts` passes with new coverage

---

### Task 4 — Epic D: AI Planner

- **id**: v3-D-ai
- **owner**: Iris
- **priority**: P2
- **dependencies**: [v3-B-sim-core]
- **files**:
  - `src/sim/turf/ai/planner.ts` (rewrite)
  - `src/sim/turf/ai/planner-goals.ts` (extend)
  - `src/sim/turf/ai/scoring.ts` (rewrite)
  - `src/sim/turf/ai/policy.ts` (minor)
  - Pre-war collection curation (new)
- **description**: Score new actions (send_to_market, send_to_holding, modifier_swap, black_market_trade, black_market_heal). New goals (heat_management, mythic_hunt, stack_rebuild). AI-side pre-war curation that mirrors player's CardGarage decisions.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] Each file < 300 LOC
  - [ ] planner.test.ts + policy.test.ts green
  - [ ] At least 3 new goals wired into planner
  - [ ] AI curation produces a valid merge + priority plan given a collection state

---

### Task 5 — Epic E: ECS Bridge

- **id**: v3-E-ecs
- **owner**: Ollie
- **priority**: P2
- **dependencies**: [v3-B-sim-core]
- **files**:
  - `src/ecs/traits.ts` (extend)
  - `src/ecs/actions.ts` (extend)
  - `src/ecs/hooks.ts` (extend)
  - `src/ecs/world.ts` (extend)
- **description**: Action helpers for all new action kinds. Hooks: `useTurfActive`, `useTurfReserves`, `useHeat`, `useBlackMarket`, `useHolding`, `useLockup`, `useMythicPool`.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] Each file < 300 LOC
  - [ ] All new action helpers take explicit `side: 'A'|'B'` first arg
  - [ ] All new hooks exported from `src/ecs/index.ts`
  - [ ] ecs-bridge.test.ts passes

---

### Task 6 — Epic F: Data Migration

- **id**: v3-F-data
- **owner**: Dex
- **priority**: P2
- **dependencies**: [v3-A-types, v3-C-abilities]
- **files**:
  - `config/raw/cards/toughs/*.json` (audit + retag)
  - `config/raw/cards/weapons/*.json` (audit + retag)
  - `config/raw/cards/drugs/*.json` (audit + retag)
  - `config/raw/cards/mythics/*.json` (new — 10 files)
  - `src/sim/cards/schemas.ts` (extend)
  - `scripts/compile-cards.mjs` (extend)
  - `src/sim/cards/catalog.ts` (extend)
  - `src/platform/persistence/collection.ts` (extend for CardInstance + unlockDifficulty)
- **description**: Retag 100 toughs to 55/25/15/5 base-rarity distribution. Retag 50 weapons, 50 drugs to 28/12/8/2. Add `unlockDifficulty` field to all CardInstance records (default 'easy'). Create 10 mythic card JSONs covering all v0.3 RULES §11 mythic ability names (Shadow/Accountant/Architect/Informer/Ghost/Warlord/Fixer/Magistrate/Phantom/Reaper). Extend Zod schemas + compile step. Update persistence.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] `pnpm run cards:compile` emits `config/compiled/mythics.json`
  - [ ] Exactly 10 mythic JSONs, one per ability from RULES §11
  - [ ] Toughs count 55+25+15+5 = 100 (with actual count match verified)
  - [ ] Weapons count 28+12+8+2 = 50
  - [ ] Drugs count 28+12+8+2 = 50
  - [ ] Persistence migration script adds default `unlockDifficulty: 'easy'` to existing saves without data loss

---

### Task 7 — Epic F-mythic: Mythic Design Pass

- **id**: v3-F-mythic-design
- **owner**: Dex + design review chain (you)
- **priority**: P1
- **dependencies**: [v3-F-data]
- **files**:
  - `config/raw/cards/mythics/mythic-01.json` through `mythic-10.json`
  - `src/ui/affiliations/MythicSymbol.tsx` (new) — per-Mythic SVG art, hand-authored placeholder geometric.
  - `src/ui/cards/Card.tsx` (extend) — mythic border treatment + art slot.
- **description**: Full mythic design pass. Each of the 10 mythics gets a unique geometric/symbolic placeholder SVG, balanced stats (reviewed against legendary baseline), evocative name + tagline + signature ability text. **Multi-review-pass quality gate**: after initial draft, review each mythic for (a) signature ability feels game-warping, (b) stats don't obsolete the legendary tier, (c) art is visually distinct from every other mythic AND from existing legendary SVGs, (d) tagline conveys character.
- **completion_criteria**:
  - [ ] 10 mythic JSONs with `name`, `tagline`, `archetype`, `affiliation: 'freelancer'`, `power`, `resistance`, `rolledRarity: 'mythic'`, `abilities: [<signature>]`
  - [ ] 10 unique SVG placeholders in MythicSymbol component
  - [ ] Review-pass 1: initial draft committed
  - [ ] Review-pass 2: 48-hour cooldown then revise for balance feel
  - [ ] Review-pass 3: peer review by Iris (does AI score these reasonably?) and Noa (does UI render each distinctly?)
  - [ ] Mythics are NOT pack-rollable (confirmed via drop rate = 0%)

---

### Task 8 — Epic G: UI

- **id**: v3-G-ui
- **owner**: Noa
- **priority**: P2
- **dependencies**: [v3-E-ecs, v3-F-mythic-design]
- **files**:
  - `src/ui/screens/GameScreen.tsx` (rewrite)
  - `src/ui/screens/CardGarageScreen.tsx` (extend)
  - `src/ui/screens/MainMenuScreen.tsx` (minor)
  - `src/ui/board/TurfView.tsx` (rewrite)
  - `src/ui/board/TurfCompositeCard.tsx` (rewrite)
  - `src/ui/board/StackFanModal.tsx` (extend)
  - `src/ui/board/BlackMarketPanel.tsx` (new)
  - `src/ui/board/HoldingPanel.tsx` (new)
  - `src/ui/board/HeatMeter.tsx` (new)
  - `src/ui/board/MythicBadge.tsx` (new)
  - `src/ui/cards/Card.tsx` (extend for unlock-difficulty icon + border + mythic)
  - `src/App.css` (extend for new visual treatments)
- **description**: Single-lane board (opp active top, self active bottom, reserves indicator). Black Market panel (left-side tablet/desktop, modal on phone). Holding panel (top-left self area). Heat meter (centered). Mythic badge + unique art treatment. Card garage extended for unlock-difficulty tags, merge UI, auto-prioritize/auto-merge toggles. Card movement animations for draw/play/retreat/swap/send-to-market/send-to-holding/raid-snatch.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] Each file < 300 LOC
  - [ ] DOM tests green
  - [ ] Browser tests green
  - [ ] GameScreen renders 1 active opp + 1 active self + reserves indicator (not N parallel lanes)
  - [ ] HeatMeter reflects state.heat in real time
  - [ ] BlackMarketPanel shows pool; modal on phone
  - [ ] Mythic cards render with unique border + MythicSymbol art
  - [ ] Card garage shows unlock-difficulty icon per instance

---

### Task 9 — Epic H: Pack Economy + Progression

- **id**: v3-H-progression
- **owner**: Luna
- **priority**: P2
- **dependencies**: [v3-F-data]
- **files**:
  - `src/platform/persistence/profile.ts` (extend for aiCollection, mythicAssignments, warStats)
  - `src/platform/persistence/ai-profile.ts` (new)
  - `src/sim/packs/rewards.ts` (new)
  - `src/sim/packs/generator.ts` (extend for rolled rarity model + high-difficulty bonus)
  - `src/sim/packs/mythic-pool.ts` (new)
- **description**: Per-turf rewards (Absolute/Overwhelming/Decisive/Standard → 5/3/1/0 cards). War outcome rewards (Perfect/Flawless/Dominant/Won → mythic/5/3/1). High-difficulty multiplier on rolled rarity odds. AI progression mirror. Mythic pool tracking.
- **completion_criteria**:
  - [ ] `pnpm exec tsc --build` EXIT 0
  - [ ] Each file < 300 LOC
  - [ ] `computePerTurfRewards` + `computeWarOutcomeReward` covered by unit tests
  - [ ] AI earns same reward bundle as player (verified by sim-vs-sim test)
  - [ ] Mythic pool depletes correctly; escalating-currency fallback after 10 claimed

---

### Task 10 — Epic I: Tests

- **id**: v3-I-tests
- **owner**: Vera
- **priority**: P3
- **dependencies**: [v3-B, v3-C, v3-D, v3-E, v3-F, v3-G, v3-H]
- **files**: (30+ test files — see full list in `v0.3-impl.prq.md` Epic I)
- **description**: Rewrite node tests for single-lane, damage tiers, heat, market, holding, turf progression, mythic. New test files: heat, market, holding, damage, turf-progression, mythic, victory-rating. Rewrite DOM + browser tests for new layout. Rewrite 4-device e2e including new market/holding/mythic flows.
- **completion_criteria**:
  - [ ] `pnpm run test:node` green (all files, 0 failures)
  - [ ] `pnpm run test:dom` green
  - [ ] `pnpm run test:browser` green
  - [ ] `pnpm run test:e2e` green on desktop-chromium, iphone-14, pixel-7, ipad-pro-landscape
  - [ ] Node coverage: new test files for each new module (heat/market/holding/damage/turf-progression/mythic/victory-rating) minimum 80% line coverage
  - [ ] E2E specs cover market send-trade-heal cycle, holding send-bribe cycle, mythic flip-on-kill, retreat, closed-ranks, war-outcome
  - [ ] `pnpm run analysis:benchmark` reports Medium winrate in 48-52% band across 3 consecutive seeded runs (hard target)

---

### Task 11 — Epic I-benchmark: Autobalance to 48-52% Medium winrate

- **id**: v3-I-benchmark
- **owner**: Vera + iterative
- **priority**: P1 (merge-blocking)
- **dependencies**: [v3-I-tests]
- **files**:
  - `src/data/ai/turf-sim.json` (tunable knobs)
  - `sim/reports/turf/balance-history.json` (output)
- **description**: Run `pnpm run analysis:autobalance` iteratively until the Medium AI-vs-AI winrate converges in 48-52%. Tune heat coefficients, damage tiers, bribe probabilities, raid multipliers, closed-ranks defensive bonus curve. Persist new baseline.
- **completion_criteria**:
  - [ ] `pnpm run analysis:benchmark` reports Medium winrate ∈ [0.48, 0.52] for 3 consecutive seeded runs
  - [ ] `pnpm run analysis:lock:persist` succeeds and updates balance-history.json
  - [ ] `pnpm run test:release` passes (release gate)
  - [ ] No heat/damage/raid coefficient hardcoded in sim code (all in turf-sim.json)

---

### Task 12 — Epic J: Docs + CI

- **id**: v3-J-docs
- **owner**: Maven
- **priority**: P3
- **dependencies**: [v3-I-tests, v3-I-benchmark]
- **files**:
  - `docs/ARCHITECTURE.md`
  - `docs/DESIGN.md`
  - `docs/PRODUCTION.md`
  - `docs/VISUAL_REVIEW.md`
  - `CLAUDE.md`
  - `README.md`
  - `CHANGELOG.md`
  - `release-please-config.json` (bump to v0.3.0)
  - `.github/workflows/ci.yml` (verify curated-sweep stays fast)
- **description**: Sync all authoritative docs to v0.3. v0.3.0 CHANGELOG entry. Major-version bump. CI workflow verifies fast curated-sweep test (no timeout regression).
- **completion_criteria**:
  - [ ] ARCHITECTURE.md reflects new modules (heat.ts, market.ts, holding.ts)
  - [ ] DESIGN.md pivots captured (single-lane progression + heat philosophy)
  - [ ] PRODUCTION.md has v0.3 release checklist
  - [ ] CHANGELOG.md v0.3.0 entry lists all breaking changes
  - [ ] release-please-config.json version bumped to 0.3.0
  - [ ] CI ci.yml verified in green state before merge

---

### Task 13 — Epic K: Open PR

- **id**: v3-K-pr
- **owner**: me
- **priority**: P0 (final)
- **dependencies**: [v3-J-docs, v3-I-benchmark]
- **description**: Open squash-mergeable PR against main. Body covers all 10 epics with acceptance criteria checkboxes. Wait for CI green across all 4 device profiles + core + browser + CodeQL.
- **completion_criteria**:
  - [ ] PR opened with comprehensive body (mirror this file's structure)
  - [ ] All CI checks green (core, browser, CodeQL, Analyze)
  - [ ] All e2e projects green (desktop-chromium, iphone-14, pixel-7, ipad-pro-landscape)
  - [ ] No blocking reviews outstanding
  - [ ] Squash-merged to main
  - [ ] Feature branch deleted

---

## Execution Plan

**Dependency graph:**

```
P0 (PR #3 merged + new branch)
 └→ v3-A-types
     ├→ v3-B-sim-core
     │   ├→ v3-D-ai ──┐
     │   └→ v3-E-ecs ──┼→ v3-I-tests → v3-I-benchmark → v3-J-docs → v3-K-pr
     ├→ v3-C-abilities ┤
     │   └→ v3-F-data ──┘
     │       └→ v3-F-mythic-design
     │           └→ v3-G-ui ─────────┘
     │               └→ v3-H-progression ──┘
```

Critical path: P0 → A → B → I-benchmark → J → K. Everything else parallelizes once A+B land.

## Failure Handling

- **Stop-on-failure: NO** for parallel lanes. If G (UI) hits a snag, H (pack economy) keeps going — they don't share files.
- **Stop-on-failure: YES** for blocking foundational epics:
  - If A fails → halt (everyone blocks on it).
  - If B fails → halt (D, E both block on it).
  - If I-benchmark fails to converge → stop K (merge-blocking).
- All other failures → document as task-level blocker, continue siblings, human-review queue.

## Batch Metadata

```yaml
batch_name: v0.3-full-rewrite
mode: parallel_specialists
priority_strategy: auto_by_dependencies
stop_on_failure: non_blocking_only
completion: open_pr_to_main_from_feat_v0.3-full-rewrite

task_count: 13
critical_path_length: 7
estimated_parallel_wall_time: 1-2 weeks
estimated_sequential_wall_time: 4-6 weeks

agents:
  Rex: [v3-A-types]
  Maya: [v3-B-sim-core]
  Kira: [v3-C-abilities]
  Iris: [v3-D-ai]
  Ollie: [v3-E-ecs]
  Dex: [v3-F-data, v3-F-mythic-design]
  Noa: [v3-G-ui]
  Luna: [v3-H-progression]
  Vera: [v3-I-tests, v3-I-benchmark]
  Maven: [v3-J-docs]
```

## Start command

Once precondition gate passes:

```bash
/task-batch docs/plans/v0.3-task-batch.md
```
